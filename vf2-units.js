/* Compatibility for cached lesson pages. Load only the current lesson's data. */
(() => { const script=document.currentScript; const declared=Number(document.documentElement.dataset.unit); const match=location.pathname.match(/vf2-u(\d{2})-live/i); const unit=declared||Number(match?.[1]); if(!unit||!script)return; const source=new URL('./vf2-u'+String(unit).padStart(2,'0')+'-live/unit-data.js',script.src).href; if(document.readyState==='loading'){document.write('<script src="'+source+'"></scr'+'ipt>');}else{const loader=document.createElement('script');loader.src=source;loader.async=false;document.head.append(loader);} })();
