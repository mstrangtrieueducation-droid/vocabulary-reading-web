/* Course navigation has been removed. Cached lesson pages also remain isolated. */
(() => { document.querySelectorAll('#vf-course-navigation,.vf-course-nav').forEach(node=>node.remove()); document.body?.classList.remove('vf-course-test-page','vf-course-reading-test-page'); })();
